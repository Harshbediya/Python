# keyword predefined words in Python. It is not allowed to use these words as variable names.

# if, else, elif, for, while, break, continue, pass, def, return, class, try, except, finally, raise, import, 
# from, as, with, lambda, yield, global, nonlocal, assert, del, and, or, not, is, in

# It is 35 keywords in Python .

# It is important to note that keywords are case-sensitive, meaning that they must be
# written in lowercase letters. For example, "if" is a keyword, but "If" or "IF" are not.

# In Python, keywords are reserved words that have special meanings and cannot be used as identifiers (variable names, function names, etc.). 
# The `keyword` module provides a list of all the keywords in Python.

# print("List of keywords in Python:")
# print(values,sep="\n",end="\n")
# sep = specifies the separator between values, and end specifies what to print at the end of the output.
# By default, sep is a space and end is a newline character.

# sep full form is separator and end full form is end character.

print(100, 200, 300, sep=",", end=" \n") #next line how to print multiple values in a single line with a custom separator and end character.
# In this case, the values 100, 200, and 300 are printed with a comma as the separator and a space as the end character.

print(400, 500, 600, sep=",", end=" \n") #next line how to print multiple values in a single line with a custom separator and end character.
# In this case, the values 400, 500, and 600 are printed with a comma as the separator and a space as the end character.